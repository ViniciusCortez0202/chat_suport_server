Controller = () => {
    createAuthSocket = (value) => {
    }

    insertMessage = (value) => {

    }

    sendMessage = (value) => {

    }

    return { createAuthSocket, insertMessage, sendMessage }
}

module.exports = Controller;
